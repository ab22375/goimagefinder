# TODO

[ ] Analyze the pro/cons of he app using an arbitrary sql atabase (postgresql, mariadb, mysql) instead of sqlite.

[ ] Analyze the possibility of storing the database on a remote server on cloud

[ ] Create a database query page, to query the records of the database with sql, see the already entered prefixes, delete records by prefix or single records

[ ] Modify the prefix input field into a select box where already existing prefixes found in the database are presented in the list and are selectable

[ ] Create a table to manage external resources to which single images are linked. Then when performing a search, the user can select which external resources to use for the search

